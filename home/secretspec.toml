# Home-manager SecretSpec declarations (values live in private pass store).
#
# pass paths: secretspec/shared/default/<NAME>

[project]
name = "dendritic-home"
revision = "1.0"
require_reason = false

[providers]
pass = "pass://secretspec/shared/{profile}/{key}"

[profiles.default]
SHIT_PASSWORD = { description = "Test secret written to ~/.shit on activation", providers = ["pass"] }
PEE_PASSWORD = { description = "Test secret written to ~/.pee on activation", providers = ["pass"] }
GH_TOKEN = { description = "Legacy classic GitHub PAT (fallback if App not bootstrapped)", providers = ["pass"] }
GH_APP_CLIENT_ID = { description = "GitHub App client ID for API token minting", providers = ["pass"] }
GH_APP_CLIENT_SECRET = { description = "GitHub App client secret", providers = ["pass"] }
GH_APP_ID = { description = "GitHub App numeric id", providers = ["pass"] }
GH_APP_PRIVATE_KEY = { description = "GitHub App PEM private key", providers = ["pass"] }
GH_REFRESH_TOKEN = { description = "GitHub App user refresh_token (ghr_) — mint access via API", providers = ["pass"] }
FLAKEHUB_TOKEN = { description = "FlakeHub token for determinate-nixd / fh login", providers = ["pass"] }
GCLOUD_CLIENT_ID = { description = "Google OAuth client ID (defaults to gcloud SDK client if unset)", providers = ["pass"] }
GCLOUD_CLIENT_SECRET = { description = "Google OAuth client secret", providers = ["pass"] }
GCLOUD_REFRESH_TOKEN = { description = "Google OAuth refresh_token — mint access via oauth2.googleapis.com", providers = ["pass"] }
GCLOUD_ACCOUNT = { description = "Google account email for gcloud config set account", providers = ["pass"] }
GCLOUD_PROJECT = { description = "Default GCP project id for gcloud config set project", providers = ["pass"] }
GCLOUD_SA_KEY = { description = "Service-account JSON key (fallback if OAuth not bootstrapped)", providers = ["pass"] }
VERCEL_REFRESH_TOKEN = { description = "Vercel CLI OAuth refresh_token (vcr_) — mint access via api.vercel.com", providers = ["pass"] }
VERCEL_TOKEN = { description = "Static Vercel PAT fallback (if OAuth not bootstrapped)", providers = ["pass"] }
VERCEL_TEAM_ID = { description = "Optional default Vercel team id for CLI scope", providers = ["pass"] }
Bubbles = { description = "Home Wi-Fi (Bubbles) WPA-PSK — dendritic.wifi", providers = ["pass"] }
WIFI_Luke_Skydumper = { description = "Wi-Fi PSK: Luke Skydumper", providers = ["pass"] }
WIFI_Myanmar_5G = { description = "Wi-Fi PSK: Myanmar-5G", providers = ["pass"] }
WIFI_Myanmar = { description = "Wi-Fi PSK: Myanmar", providers = ["pass"] }
WIFI_Everyday_Im_Buffering = { description = "Wi-Fi PSK: Everyday Im Buffering", providers = ["pass"] }
WIFI_Sunburst = { description = "Wi-Fi PSK: Sunburst", providers = ["pass"] }
WIFI_Wifi_From_Heaven = { description = "Wi-Fi PSK: Wifi-From-Heaven", providers = ["pass"] }
WIFI_Indaba_Guest = { description = "Wi-Fi PSK: Indaba Guest", providers = ["pass"] }
WIFI_Arctos_Coffee = { description = "Wi-Fi PSK: Arctos Coffee", providers = ["pass"] }
WIFI_BreakEspresso_5 = { description = "Wi-Fi PSK: BreakEspresso-5", providers = ["pass"] }
WIFI_LadderCoffee_5 = { description = "Wi-Fi PSK: LadderCoffee_5", providers = ["pass"] }
WIFI_PF_Guest_WiFi = { description = "Wi-Fi PSK: PF Guest WiFi (Planet Fitness)", providers = ["pass"] }
WIFI_Clyde_Coffee_Guest = { description = "Wi-Fi PSK: Clyde Coffee Guest", providers = ["pass"] }
EDUROAM_IDENTITY = { description = "EWU eduroam identity (user@ewu.edu)", providers = ["pass"] }
EDUROAM_PASSWORD = { description = "EWU eduroam SSO password", providers = ["pass"] }
EDUROAM_CA = { description = "EWU eduroam trust PEM bundle (InCommon + USERTrust)", providers = ["pass"] }
EDUROAM_PROFILE = { description = "EWU eduroam EAP metadata JSON (server mask, methods)", providers = ["pass"] }
DISCORD_TOKEN = { description = "Discord bot token for GuildForge MCP (@guildforge/mcp)", providers = ["pass"] }
GUILD_ID = { description = "Discord guild (server) ID scoped for GuildForge MCP", providers = ["pass"] }
MACRDP_PASSWORD = { description = "macrdp NLA password (mba RDP server)", providers = ["pass"] }
LOGIN_PASSWORD = { description = "Shared NixOS + Windows local login password (sliceanddice)", providers = ["pass"] }
