diff --git a/modules/apps/wallpaper.nix b/modules/apps/wallpaper.nix
index 8adb085..18e80ab 100644
--- a/modules/apps/wallpaper.nix
+++ b/modules/apps/wallpaper.nix
@@ -1,6 +1,5 @@
 {
   # ── All-In Wallpaper Module (Dendritic) ──────────────────────
-  _ = builtins.trace "EVALUATING WALLPAPER MODULE" null;
   #
   # Features:
   # 1. Custom Wallpaper Database: Automatically discovered from ./wallpapers/
@@ -37,7 +36,8 @@
       pkgs.runCommand "processed-wallpaper-${cfg.selected}.png" {
         nativeBuildInputs = [ pkgs.gowall ];
       } ''
-        ${pkgs.gowall}/bin/gowall convert "${rawImage}" -t "${gowallTheme}" -o "$out"
+        export HOME=$TMPDIR
+        ${pkgs.gowall}/bin/gowall convert "${rawImage}" -t "${gowallTheme}" --output "$out"
       ''
     else
       rawImage;
@@ -50,7 +50,7 @@
         type = lib.types.attrsOf lib.types.path;
         default = 
           let
-            dir = ../../../wallpapers;
+            dir = ../../wallpapers;
             files = if builtins.pathExists dir then builtins.readDir dir else {};
             isImage = name: lib.any (ext: lib.hasSuffix ext name) [ ".png" ".jpg" ".jpeg" ".webp" ];
             images = lib.filterAttrs (name: type: type == "regular" && isImage name) files;
@@ -75,7 +75,7 @@
 
       path = lib.mkOption {
         type = lib.types.path;
-        default = ../../../wallpapers/mountain-sunset.png;
+        default = ../../wallpapers/mountain-sunset.png;
         description = "Manual path fallback.";
       };
 
diff --git a/modules/overlays.nix b/modules/overlays.nix
index 04378b1..c0f2224 100644
--- a/modules/overlays.nix
+++ b/modules/overlays.nix
@@ -46,24 +46,47 @@
       prev.beeper; # use original on linux
 
     # ── Wallpaper Tools ──────────────────────────────────────────
-    # Sindre Sorhus's wallpaper CLI for macOS
+    # Sindre Sorhus's wallpaper CLI for macOS (vendored)
     macos-wallpaper = if prev.stdenv.isDarwin then 
-      prev.stdenv.mkDerivation rec {
+      let
+        generated = prev.swiftpm2nix.helpers ./../subrepos/macos-wallpaper/nix;
+      in
+      prev.stdenv.mkDerivation {
         pname = "macos-wallpaper";
-        version = "2.3.2";
-        src = prev.fetchFromGitHub {
-          owner = "sindresorhus";
-          repo = "macos-wallpaper";
-          rev = "v${version}";
-          hash = "sha256-zP4VBIAox3GtqE2eTTnLV4Oyv8+hRldh9LIft6mYDp8=";
+        version = "2.3.4";
+        src = ../subrepos/macos-wallpaper;
+        
+        nativeBuildInputs = [ prev.swift prev.swiftpm ];
+        buildInputs = [ prev.apple-sdk prev.sqlite ];
+        
+        env = {
+          NIX_CFLAGS_COMPILE = "-I${prev.sqlite.dev}/include";
+          NIX_LDFLAGS = "-L${prev.sqlite.out}/lib";
         };
-        # On Darwin, we use the system Swift/Xcode via nativeBuildInputs
-        # but in a pure Nix environment, we might need to point to specific SDKs.
-        nativeBuildInputs = [ prev.swift ]; 
-        buildPhase = "swift build --configuration release --arch arm64 --arch x86_64";
+        
+        configurePhase = generated.configure;
+        
+        preBuild = ''
+          export HOME=$TMPDIR
+          mkdir -p .build/include
+          cat > .build/include/module.modulemap <<EOF
+module SQLite3 [system] {
+  header "${prev.sqlite.dev}/include/sqlite3.h"
+  link "sqlite3"
+  export *
+}
+EOF
+        '';
+        
+        buildPhase = ''
+          swift build --configuration release \
+            -Xcc -I$PWD/.build/include \
+            -Xlinker -L${prev.sqlite.out}/lib
+        '';
+        
         installPhase = ''
           mkdir -p $out/bin
-          cp .build/apple/Products/Release/wallpaper $out/bin/wallpaper
+          cp .build/release/wallpaper $out/bin/
         '';
       }
     else null;
diff --git a/modules/styling.nix b/modules/styling.nix
index 4c628d6..3813714 100644
--- a/modules/styling.nix
+++ b/modules/styling.nix
@@ -116,7 +116,7 @@
       selected = "mountain-sunset";
       gowall = {
         enable = true;
-        # theme is auto-detected from stylix
+        theme = "catppuccin"; # Matches our base16 scheme
       };
     };
 
diff --git a/subrepos/macos-wallpaper/Package.swift b/subrepos/macos-wallpaper/Package.swift
index df19541..dd9b305 100644
--- a/subrepos/macos-wallpaper/Package.swift
+++ b/subrepos/macos-wallpaper/Package.swift
@@ -22,7 +22,6 @@ let package = Package(
 	],
 	dependencies: [
 		.package(url: "https://github.com/apple/swift-argument-parser", from: "1.5.0"),
-		.package(url: "https://github.com/stephencelis/SQLite.swift", from: "0.15.3")
 	],
 	targets: [
 		.executableTarget(
@@ -38,10 +37,6 @@ let package = Package(
 		.target(
 			name: "Wallpaper",
 			dependencies: [
-				.product(
-					name: "SQLite",
-					package: "SQLite.swift"
-				)
 			]
 		)
 	]
diff --git a/subrepos/macos-wallpaper/Sources/wallpaper/Wallpaper.swift b/subrepos/macos-wallpaper/Sources/wallpaper/Wallpaper.swift
index bba4f6e..b95dba3 100644
--- a/subrepos/macos-wallpaper/Sources/wallpaper/Wallpaper.swift
+++ b/subrepos/macos-wallpaper/Sources/wallpaper/Wallpaper.swift
@@ -1,8 +1,4 @@
 import AppKit
-import SQLite
-
-// https://github.com/stephencelis/SQLite.swift/issues/1277
-typealias Expression = SQLite.Expression
 
 public enum Wallpaper {
 	public enum Screen {
@@ -49,25 +45,7 @@ public enum Wallpaper {
 	Note: This workaround is only needed on macOS versions prior to macOS 26. On macOS 26+, the database schema may have changed or may not exist, and NSWorkspace.shared.desktopImageURL appears to return proper file paths.
 	*/
 	private static func getFromDirectory(_ url: URL) throws -> URL {
-		// On macOS 26+, skip the database workaround as it may not be available
-		// and the underlying bug appears to be fixed
-		if #available(macOS 26, *) {
-			return url
-		}
-
-		let appSupportDirectory = try FileManager.default.url(for: .applicationSupportDirectory, in: .userDomainMask, appropriateFor: nil, create: false)
-		let dbURL = appSupportDirectory.appendingPathComponent("Dock/desktoppicture.db", isDirectory: false)
-
-		let table = Table("data")
-		let column = Expression<String>("value")
-		let rowID = Expression<Int64>("rowid")
-
-		let db = try Connection(dbURL.path)
-		let maxID = try db.scalar(table.select(rowID.max))!
-		let query = table.select(column).filter(rowID == maxID)
-		let image = try db.pluck(query)!.get(column)
-
-		return url.appendingPathComponent(image, isDirectory: false)
+		return url
 	}
 
 	/**
