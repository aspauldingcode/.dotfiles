# Mobile Fish Configuration
# Basic shell configuration for mobile devices

# Set up basic aliases
alias ll 'ls -la'
alias la 'ls -A'
alias l 'ls -CF'

# History settings
set -g fish_history_size 1000
