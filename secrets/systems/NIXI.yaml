# System-specific secrets for NIXI (Intel Mac)
# This file contains secrets specific to the NIXI system configuration

# Example secrets - replace with actual values
example_secret: "placeholder_value"
api_key: "placeholder_api_key"
database_password: "placeholder_password"

# System-specific configuration
hostname: "NIXI"
system_type: "x86_64-darwin"
environment: "development"

# Network configuration secrets
wifi_password: "placeholder_wifi_password"
vpn_config: "placeholder_vpn_config"

# Service credentials
service_token: "placeholder_service_token"
backup_key: "placeholder_backup_key"